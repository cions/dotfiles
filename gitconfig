# vim: set ft=gitconfig:
[core]
	editor = vim
	excludesFile = ~/.gitignore
[protocol]
	version = 2
[diff]
	algorithm = histogram
[merge]
	defaultToUpstream = true
	ff = false
	tool = vimdiff
[pull]
	ff = only
[rebase]
	autoSquash = true
[status]
	relativePaths = false
[alias]
	b = switch
	nb = switch --create
	co = checkout
	cob = checkout -b
	s = status
	ss = status --short
	d = diff
	ds = diff --stat
	dn = diff --name-only
	dc = diff --cached
	dcs = diff --cached --stat
	dcn = diff --cached --name-only
	a = add
	aa = add --all
	au = add --update
	ae = add --edit
	ap = add --patch
	untrack = rm --cached --
	c = commit
	cm = commit -m
	ca = !git add --all && git commit
	cam = !git add --all && git commit -m
	cu = !git add --update && git commit
	cum = !git add --update && git commit -m
	amend = commit --amend --reuse-message=HEAD --reset-author
	wip = stash save --include-untracked
	l = log --pretty=simple --no-merges
	la = log --pretty=simple --no-merges --all
	lg = log --pretty=simple --graph
	lga = log --pretty=simple --graph --all
	ll = log --pretty=detail --graph
	lla = log --pretty=detail --graph --all
	ls = log --pretty=detail --graph --stat
	lsa = log --pretty=detail --graph --stat --all
	ld = log --pretty=detail --graph --patch
	lda = log --pretty=detail --graph --patch --all
	ls-unmerged = diff --diff-filter=U --name-only
	resolve = !git ls-unmerged -z | xargs --null --open-tty --no-run-if-empty ${EDITOR:-vi} --
	ours = restore --ours
	theirs = restore --theirs
[format]
	pretty = detail
[pretty]
	simple = %C(yellow)%h%C(reset) %s %C(green)(%cr)%C(reset) %C(bold blue)[%an]%C(reset)%C(auto)%d%C(reset)
	detail = %C(yellow)commit %H%C(auto)%d%C(reset)%nauthor %an <%ae>%ndate   %ci %C(green)(%cr)%C(reset)%n%n%w(72,4,8)%B%w(0)
[color "diff"]
	commit = bold yellow
	context = white
	frag = bold cyan
	func = cyan
	meta = bold white
	new = green
	old = red
	whitespace = red reverse
[color "grep"]
	context = normal
	filename = green
	function = yellow
	linenumber = cyan
	match = bold red
	selected = bold normal
	separator = cyan
[pager]
	diff = $(command -v diff-highlight cat | head -1) | less
	log = $(command -v diff-highlight cat | head -1) | less
	show = $(command -v diff-highlight cat | head -1) | less
[user]
	name = cions
	email = gh.cions@gmail.com
